% Generated by tablambda_examples._examples (tablambda-defun-examples). Do not edit;
% regenerate with: python -m tablambda_examples._examples

\medskip\noindent\textbf{Identity.}\quad $\lambda x.\, x$
\begin{lstlisting}
@interned
class vg_61b34437d45d82e0:

    def __call__(self, a):
        return a
compiled = vg_61b34437d45d82e0()
\end{lstlisting}

\medskip\noindent\textbf{The constant combinator $K$.}\quad $\lambda x.\, \lambda y.\, x$
\begin{lstlisting}
@interned
class vg_513d3b50ad4efcd5:
    cap_0: Lambda

    def __call__(self, a):
        return self.cap_0

@interned
class vg_ed8877638757d1e5:

    def __call__(self, a):
        return vg_513d3b50ad4efcd5(a)
compiled = vg_ed8877638757d1e5()
\end{lstlisting}

\medskip\noindent\textbf{Church numeral $2$.}\quad $\lambda x.\, \lambda y.\, x\, (x\, y)$
\begin{lstlisting}
@interned
class vg_668ed7a4d73636cd:

    def __call__(self, a):
        return vg_dd3a9ec0f1f21e07(a)

@interned
class vg_dd3a9ec0f1f21e07:
    cap_0: Lambda

    def __call__(self, a):
        return Thunk(self.cap_0, Thunk(self.cap_0, a))
compiled = vg_668ed7a4d73636cd()
\end{lstlisting}

\medskip\noindent\textbf{Successor.}\quad $\lambda x.\, \lambda y.\, \lambda z.\, y\, (x\, y\, z)$
\begin{lstlisting}
@interned
class vg_024ecac0007beeed:
    cap_0: Lambda

    def __call__(self, a):
        return vg_ac1702da3e5f1e19(a, self.cap_0)

@interned
class vg_797083f6435071e8:

    def __call__(self, a):
        return vg_024ecac0007beeed(a)

@interned
class vg_ac1702da3e5f1e19:
    cap_0: Lambda
    cap_1: Lambda

    def __call__(self, a):
        return Thunk(self.cap_0, Thunk(Thunk(self.cap_1, self.cap_0), a))
compiled = vg_797083f6435071e8()
\end{lstlisting}

\medskip\noindent\textbf{Addition.}\quad $\lambda x.\, \lambda y.\, \lambda z.\, \lambda w.\, x\, z\, (y\, z\, w)$
\begin{lstlisting}
@interned
class vg_3a0aff5bf1368a2c:
    cap_0: Lambda

    def __call__(self, a):
        return vg_523cab64babb50de(self.cap_0, a)

@interned
class vg_523cab64babb50de:
    cap_0: Lambda
    cap_1: Lambda

    def __call__(self, a):
        return vg_fc570d6bf3eb9d38(self.cap_0, a, self.cap_1)

@interned
class vg_98b78b6a8655531f:

    def __call__(self, a):
        return vg_3a0aff5bf1368a2c(a)

@interned
class vg_fc570d6bf3eb9d38:
    cap_0: Lambda
    cap_1: Lambda
    cap_2: Lambda

    def __call__(self, a):
        return Thunk(Thunk(self.cap_0, self.cap_1), Thunk(Thunk(self.cap_2, self.cap_1), a))
compiled = vg_98b78b6a8655531f()
\end{lstlisting}

\medskip\noindent\textbf{Multiplication.}\quad $\lambda x.\, \lambda y.\, \lambda z.\, x\, (y\, z)$
\begin{lstlisting}
@interned
class vg_1df5f797ec547eb1:
    cap_0: Lambda

    def __call__(self, a):
        return vg_59c47787408da0bf(self.cap_0, a)

@interned
class vg_59c47787408da0bf:
    cap_0: Lambda
    cap_1: Lambda

    def __call__(self, a):
        return Thunk(self.cap_0, Thunk(self.cap_1, a))

@interned
class vg_f0db4cb95e6c20bd:

    def __call__(self, a):
        return vg_1df5f797ec547eb1(a)
compiled = vg_f0db4cb95e6c20bd()
\end{lstlisting}

\medskip\noindent\textbf{Zero test.}\quad $\lambda x.\, x\, (\lambda y.\, \lambda z.\, \lambda w.\, w)\, (\lambda y.\, \lambda z.\, y)$
\begin{lstlisting}
@interned
class vg_513d3b50ad4efcd5:
    cap_0: Lambda

    def __call__(self, a):
        return self.cap_0

@interned
class vg_61b34437d45d82e0:

    def __call__(self, a):
        return a

@interned
class vg_9eca90c294b53d2f:

    def __call__(self, a):
        return vg_61b34437d45d82e0()

@interned
class vg_a34392661eb8de00:

    def __call__(self, a):
        return Thunk(Thunk(a, vg_a834fc82a7024f4f()), vg_ed8877638757d1e5())

@interned
class vg_a834fc82a7024f4f:

    def __call__(self, a):
        return vg_9eca90c294b53d2f()

@interned
class vg_ed8877638757d1e5:

    def __call__(self, a):
        return vg_513d3b50ad4efcd5(a)
compiled = vg_a34392661eb8de00()
\end{lstlisting}

\medskip\noindent\textbf{Self-application.}\quad $\lambda x.\, x\, x$
\begin{lstlisting}
@interned
class vg_e18cec08b71c2b6d:

    def __call__(self, a):
        return Thunk(a, a)
compiled = vg_e18cec08b71c2b6d()
\end{lstlisting}
